<h1>Student Index</h1>

<h2><%= pluralize(@students.count, "student") %></h2>

<%= render partial: 'search', locals: {search_term: @query} %>

<%= render partial: 'student', collection: @students %>

<%# console %>